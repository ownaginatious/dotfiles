- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.zshrc: zsh/zshrc
    ~/.i3:
        path: i3/
        create: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
